## CipherDocs.UI

Next.js frontend for CipherDocs.

### Run locally
1. Copy env:
   - `.env.example` -> `.env.local`
2. Install and run:
   - `npm install`
   - `npm run dev`

Env:
- `NEXT_PUBLIC_API_BASE` should point to CipherDocs.API (example: `http://127.0.0.1:4000`)

Labs:
- See `docs/labs/`